import 'package:flutter/cupertino.dart';

/// style for the labels in the tab bar
const tabHeading = TextStyle(fontSize: 20.0, fontWeight: FontWeight.bold);

/// style for the percentage in the upload tile
const percentageText = TextStyle(
    color: Color(0xFF00B3A6), fontSize: 20, fontWeight: FontWeight.bold);

/// style used for regular text in the app
const regularText = TextStyle(fontSize: 14.0);
